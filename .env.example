# Fast UI development uses deterministic weather and needs no API key.
VITE_WEATHER_MOCK=true
VITE_DISPLAY_TIMEZONE=Europe/London

# In Vite development, preview commute mode at any time with:
# ?previewBus=station|outbound|inbound|stale|untracked
